output{
    elasticsearch {
        hosts => ["https://localhost:9200"]
        # user => "elastic"
        # password => "elastic"
        # ssl => true
        # ssl_certificate_verification => false
        # template_name => "lm-tia-api"
        index => "%{[@metadata][index_name]}-%{[fields][env]}-%{[fields][appName]}-logs-%{+YYYY-MM-dd}"
    }
}